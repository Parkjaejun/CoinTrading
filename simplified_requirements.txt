# OKX 자동매매 시스템 - 완전한 라이브러리 목록

# 필수 라이브러리
requests==2.31.0
websocket-client==1.6.4

# 데이터 처리
pandas==2.1.4
numpy==1.24.3

# 날짜/시간 처리
python-dateutil==2.8.2

# 알림 시스템
urllib3==2.0.7

# JSON 처리 (내장 모듈이지만 명시)
# json - 내장 모듈

# 암호화 (API 서명용, 내장 모듈)
# hmac - 내장 모듈
# hashlib - 내장 모듈
# base64 - 내장 모듈

# 멀티스레딩 (내장 모듈)
# threading - 내장 모듈
# queue - 내장 모듈

# 시스템 관련 (내장 모듈)
# os - 내장 모듈
# sys - 내장 모듈
# time - 내장 모듈
# signal - 내장 모듈
# logging - 내장 모듈

# 이메일 (내장 모듈)
# smtplib - 내장 모듈
# email - 내장 모듈

# 정규표현식 (내장 모듈)
# re - 내장 모듈

# 컬렉션 (내장 모듈)
# collections - 내장 모듈

# 타입 힌팅 (내장 모듈)
# typing - 내장 모듈

# 선택적 라이브러리 (성능 향상용)
# 더 빠른 JSON 처리를 원할 경우
# orjson==3.9.10

# 더 강력한 HTTP 클라이언트를 원할 경우  
# httpx==0.25.2

# 메모리 사용량 모니터링을 원할 경우
# psutil==5.9.6

# 더 정교한 로깅을 원할 경우
# structlog==23.2.0

# 설정 관리를 더 체계적으로 하고 싶을 경우
# pydantic==2.5.0

# 비동기 처리를 원할 경우 (고급 사용자용)
# asyncio - 내장 모듈
# aiohttp==3.9.1

# 데이터 시각화를 원할 경우 (백테스팅 결과 차트)
# matplotlib==3.8.2
# plotly==5.17.0

# 더 정교한 수치 계산을 원할 경우
# scipy==1.11.4

# 백테스팅 성능 향상을 원할 경우
# numba==0.58.1

# 개발/디버깅용 라이브러리
# ipython==8.18.1
# pytest==7.4.3

# 보안 강화용 라이브러리
# cryptography==41.0.7