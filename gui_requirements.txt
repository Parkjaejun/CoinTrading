# GUI 전용 라이브러리 목록
# gui_requirements.txt

# 기존 요구사항 (기본 시스템)
requests==2.31.0
websocket-client==1.6.4
pandas==2.1.4
numpy==1.24.3
python-dateutil==2.8.2

# GUI 전용 라이브러리
PyQt5==5.15.9
PyQtGraph==0.13.3

# 시스템 모니터링 (선택사항)
psutil==5.9.6

# 차트 및 시각화 (추가 옵션)
matplotlib==3.8.2

# 설정 파일 처리
configparser==5.3.0

# GUI 스타일링 (선택사항)
qdarkstyle==3.2.1

# 아이콘 및 리소스 (선택사항)
qtawesome==1.2.3

# 설치 명령:
# pip install -r gui_requirements.txt
#
# 또는 기본 + GUI 라이브러리만:
# pip install PyQt5==5.15.9 pyqtgraph==0.13.3 psutil==5.9.6